"""Единственный тип ошибки сервера.

Живёт отдельным модулем, а не в archive.py, потому что нужен и writer.py -
тот по слоям ниже архива, и импорт из archive.py дал бы цикл. Для совместимости
archive.py его реэкспортирует, так что старые импорты продолжают работать.
"""

from __future__ import annotations


class FscpError(Exception):
    """Ошибка, которую можно показать пользователю без трейсбека."""
